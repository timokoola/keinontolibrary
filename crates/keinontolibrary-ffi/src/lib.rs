//! `keinontolibrary-ffi` — binding scaffold exposing `decline`/`paradigm` over a
//! foreign-function interface (`UniFFI` for Swift; feature-gated wasm and `PyO3`).
//!
//! Scaffold only; bindings land in Phase 5. The crate type widens to `cdylib`/
//! `staticlib` once `UniFFI` is wired in.
