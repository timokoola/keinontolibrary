//! `keinontolibrary-ingest` — offline pipeline turning the Kotus word list and the
//! Voikko JSONL corpus into the packed lookup artifact. Implemented in Phase 2.

fn main() {
    eprintln!("keinontolibrary-ingest: not yet implemented (Phase 2)");
}
