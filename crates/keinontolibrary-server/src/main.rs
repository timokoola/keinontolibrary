//! `keinontolibrary-server` — axum HTTP service: `/decline`, `/paradigm`, `/admin/*`,
//! `/healthz`, `/about`. Implemented in Phase 5.

fn main() {
    eprintln!("keinontolibrary-server: not yet implemented (Phase 5)");
}
